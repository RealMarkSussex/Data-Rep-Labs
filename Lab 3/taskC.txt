Consider the document namespaces.xml and answer the following questions:

Which namespaces do the elements foo, bar, and baz belong to?

foo belongs to the namespace B, bar belongs to the namespace C and bas doesn't belong to a namespace

Which namespaces do the attributes qux, quux, and corge belong to?

qux doesn't belong to a namespace, quux belongs to the namespace B and corge belongs to the namespace F